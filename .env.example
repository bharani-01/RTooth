PORT=5000
SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_ANON_KEY=your-anon-key
# Optional: service role key if backend administrative actions are needed (like deleting users, updating profiles without auth constraints, etc.)
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
